# frozen_string_literal: true

source "https://rubygems.org"

gem "json", "~> 2.6"
gem "image_processing", "~> 1.12"


#
# This is the code for the Lambda resising kosube image
# Ruby 3.2 runtime
# Just zip this shit using below command and sent it to AWS lambda
#
# Install gem localy, so you can include in zip file :
# bundle config set --local path 'vendor/bundle' && bundle install
#
# Reset to global bundle
# bundle config set --local system 'true'
#
# To construct zipfile to upload on lambda :
# zip -r deploy.zip app.rb vendor
